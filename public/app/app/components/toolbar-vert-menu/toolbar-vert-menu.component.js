import controller from './toolbar-vert-menu.controller';
import template from './toolbar-vert-menu.html!text';

let bindings = {
  onSelectLayersDownload: '&'
};

export default {
  controller,
  template,
  bindings
};
