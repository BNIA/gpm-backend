extends layout
include ./mixins/download-link
block content
  h1= title
  p Welcome to the layers page
  +downloadLink('application/json', rows, 'layers.json', 'Download Layers')
  //- each row in rows
    p=row.id
