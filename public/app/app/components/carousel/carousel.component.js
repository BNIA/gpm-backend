import './carousel.css!';
import controller from './carousel.controller';
import template from './carousel.html!text';

var bindings = {
  publicIds: '<'
};

export default {
  controller,
  template,
  bindings
};
